# 第七阶段：真实客户端联调与稳定性验证 - 测试报告

## 测试环境说明

### 系统配置
- **服务地址**: http://localhost:3000
- **API Key**: (待用户提供)
- **管理员 Token**: (待用户提供)
- **数据库**: SQLite (One API 默认)
- **Redis**: (待确认)

### Fallback 配置摘要
```json
{
  "enabled": true,
  "virtual_models": {
    "jiaobcxvcv-auto": {
      "enabled": true,
      "description": "Default auto fallback model",
      "fallback_order": [
        "primary",
        "backup_1",
        "backup_2"
      ]
    }
  },
  "deployments": {
    "primary": {
      "enabled": true,
      "channel_id": 4,
      "real_model": "openrouter/free",
      "priority": 1,
      "daily_limit_tokens": 1000000,
      "soft_limit_ratio": 0.9,
      "hard_limit_ratio": 0.98
    },
    "backup_1": {
      "enabled": true,
      "channel_id": 5,
      "real_model": "openrouter/free",
      "priority": 2,
      "daily_limit_tokens": 1000000,
      "soft_limit_ratio": 0.9,
      "hard_limit_ratio": 0.98
    },
    "backup_2": {
      "enabled": true,
      "channel_id": 6,
      "real_model": "openrouter/free",
      "priority": 3,
      "daily_limit_tokens": 1000000,
      "soft_limit_ratio": 0.9,
      "hard_limit_ratio": 0.98
    }
  }
}
```

### Channel ID 对应的真实模型
- **primary (channel_id: 4)**: openrouter/free
- **backup_1 (channel_id: 5)**: openrouter/free
- **backup_2 (channel_id: 6)**: openrouter/free

## 测试步骤

### 1. 准备工作

需要用户提供：
- [ ] One API 管理员 Token
- [ ] 确认 OpenRouter 渠道是否已在 One API 中配置 (channel_id 4, 5, 6)
- [ ] 确认 One API 运行端口 (默认 3000)

### 2. 测试内容

#### 2.1. curl 非流式测试
```bash
curl http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer <你的令牌>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "jiaobcxvcv-auto",
    "messages": [{"role": "user", "content": "Hello, please respond with a short greeting"}],
    "stream": false
  }'
```

#### 2.2. curl 流式测试
```bash
curl http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer <你的令牌>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "jiaobcxvcv-auto",
    "messages": [{"role": "user", "content": "Count to 10, and say hello"}],
    "stream": true
  }'
```

#### 2.3. LobeChat 接入
1. 在 LobeChat 设置中添加 OpenAI 兼容 API
2. Model 填写: `jiaobcxvcv-auto`
3. API Endpoint 填写: `http://localhost:3000/v1`
4. API Key 填写: (用户提供)
5. 测试对话

#### 2.4. Hermes 接入
1. 在 Hermes 设置中添加 OpenAI 兼容 API
2. Model 填写: `jiaobcxvcv-auto`
3. API Endpoint 填写: `http://localhost:3000/v1`
4. API Key 填写: (用户提供)
5. 测试对话

#### 2.5. primary 不可用测试
```bash
# 1. 先获取 primary 的状态
curl http://localhost:3000/api/fallback/states \
  -H "Authorization: Bearer <管理员令牌>"

# 2. 模拟 primary 不可用（通过其他方式）
# 方法1: 禁用 primary 渠道
# 方法2: 设置 exhausted_until
curl -X POST http://localhost:3000/api/fallback/deployments/primary/clear-exhausted \
  -H "Authorization: Bearer <管理员令牌>"

# 3. 测试虚拟模型是否能自动切换到 backup_1
curl http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer <你的令牌>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "jiaobcxvcv-auto",
    "messages": [{"role": "user", "content": "test fallback"}],
    "stream": false
  }'

# 4. 查看状态，确认 backup_1 已使用
curl http://localhost:3000/api/fallback/states \
  -H "Authorization: Bearer <管理员令牌>"
```

#### 2.6. quota exhausted 测试
```bash
# 1. 设置 primary 的 exhausted_until
curl -X POST http://localhost:3000/api/fallback/deployments/primary/reset \
  -H "Authorization: Bearer <管理员令牌>"

# 2. 模拟 primary 额度耗尽
curl -X POST "http://localhost:3000/api/fallback/deployments/primary/reset?until=2026-12-31T00:00:00Z" \
  -H "Authorization: Bearer <管理员令牌>"

# 3. 测试虚拟模型是否能自动切换到 backup_1
curl http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer <你的令牌>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "jiaobcxvcv-auto",
    "messages": [{"role": "user", "content": "test quota exhausted"}],
    "stream": false
  }'

# 4. 验证 primary 状态
curl http://localhost:3000/api/fallback/states \
  -H "Authorization: Bearer <管理员令牌>"
```

#### 2.7. 429 cooldown 测试
```bash
# 1. 设置 backup_1 的 cooldown_until
curl -X POST "http://localhost:3000/api/fallback/deployments/backup_1/reset?cooldown_until=2026-06-01T15:00:00Z" \
  -H "Authorization: Bearer <管理员令牌>"

# 2. 测试虚拟模型是否能自动切换到 backup_2
curl http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer <你的令牌>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "jiaobcxvcv-auto",
    "messages": [{"role": "user", "content": "test cooldown"}],
    "stream": false
  }'

# 3. 验证 backup_1 状态
curl http://localhost:3000/api/fallback/states \
  -H "Authorization: Bearer <管理员令牌>"
```

#### 2.8. hard limit 测试
```bash
# 1. 设置 primary 的使用量达到 hard limit
# 方法: 直接修改数据库或使用管理接口
# 这里假设我们手动设置

# 2. 测试虚拟模型是否能跳过 primary
curl http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer <你的令牌>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "jiaobcxvcv-auto",
    "messages": [{"role": "user", "content": "test hard limit"}],
    "stream": false
  }'

# 3. 验证状态
curl http://localhost:3000/api/fallback/states \
  -H "Authorization: Bearer <管理员令牌>"
```

#### 2.9. 管理接口测试
```bash
# 1. 查看所有状态
curl http://localhost:3000/api/fallback/states \
  -H "Authorization: Bearer <管理员令牌>"

# 2. 重置 deployment 状态
curl -X POST http://localhost:3000/api/fallback/deployments/backup_1/reset \
  -H "Authorization: Bearer <管理员令牌>"

# 3. 清除 exhausted
curl -X POST http://localhost:3000/api/fallback/deployments/backup_1/clear-exhausted \
  -H "Authorization: Bearer <管理员令牌>"

# 4. 清除 cooldown
curl -X POST http://localhost:3000/api/fallback/deployments/backup_1/clear-cooldown \
  -H "Authorization: Bearer <管理员令牌>"

# 5. 重载配置
curl -X POST "http://localhost:3000/api/fallback/config/reload?path=data/fallback.json" \
  -H "Authorization: Bearer <管理员令牌>"
```

#### 2.10. 普通模型兼容性测试
```bash
# 测试非虚拟模型是否能正常工作
curl http://localhost:3000/v1/models \
  -H "Authorization: Bearer <你的令牌>"

# 测试真实的 OpenRouter 模型（如果已在配置中）
curl http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer <你的令牌>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o-mini",  # 或其他真实模型
    "messages": [{"role": "user", "content": "test normal model"}],
    "stream": false
  }'
```

#### 2.11. 日志和数据库验证
```bash
# 查看 One API 日志
# 观察 fallback 相关日志：
# - "[fallback] attempt X/Y virtual model jiaobcxvcv-auto deployment ..."
# - "[fallback] deployment xxx failed (attempt X/Y): ..."
# - "[fallback] deployment xxx marked ..."

# 查看数据库状态
# sqlite3 one-api.db "SELECT * FROM fallback_deployment_states;"

# 验证字段值：
# - RequestCount, SuccessCount, ErrorCount
# - UsedTotalTokens
# - ExhaustedUntil, CooldownUntil
# - LastErrorCode, LastErrorMessage
```

## 待用户提供的信息

### 必需信息
1. **One API 管理员 Token**
   - 在 OneAPI 管理后台创建一个管理员账号，复制 token
   - 格式: Bearer xxxxx

2. **确认 OpenRouter 渠道配置**
   - 确认 channel_id 4, 5, 6 是否已在 One API 中配置
   - 如果没有，需要先在 One API 中添加 OpenRouter 渠道
   - OpenRouter 渠道配置：
     - Base URL: https://openrouter.ai/api/v1
     - API Key: sk-or-v1-OPENROUTER_KEY_REMOVED_BY_FILTER_REPO
     - Model: openrouter/free
     - 类型: OpenAI

3. **确认 Redis 配置**
   - 如果 Redis 未启用，fallback 状态功能正常（不依赖 Redis）

## 下一步操作

### 立即需要的
1. 提供 One API 管理员 Token
2. 确认 OpenRouter 渠道配置

### 可选需要的
1. 是否需要在 One API 中添加 channel_id 4, 5, 6 的 OpenRouter 渠道
2. 是否需要测试其他客户端

## 预期测试结果

### 成功标准
- ✅ curl 非流式请求成功返回
- ✅ curl 流式请求成功返回
- ✅ LobeChat 能正常接入并对话
- ✅ Hermes 能正常接入并对话
- ✅ primary 不可用时自动切换到 backup_1
- ✅ quota exhausted 时自动切换到其他 deployment
- ✅ 429 cooldown 时自动切换到其他 deployment
- ✅ hard limit 时自动跳过该 deployment
- ✅ 管理接口全部可用
- ✅ 普通模型请求不受影响
- ✅ 日志正确记录 fallback 过程
- ✅ 数据库状态正确更新

### 需要修复的问题
- 如果发现任何问题，记录到问题列表中
- 记录修复过程和结果

---

## 测试完成后的输出内容

测试完成后，请提供以下信息：

1. **测试环境说明** - 已在此文档中
2. **使用的 fallback.json 配置摘要** - 已在此文档中
3. **每个 channel_id 对应的真实模型** - 已在此文档中
4. **curl 非流式测试结果** - 粘贴实际返回内容
5. **curl 流式测试结果** - 粘贴实际返回内容
6. **LobeChat 接入结果** - 是否成功，有无错误
7. **Hermes 接入结果** - 是否成功，有无错误
8. **primary 不可用测试结果** - 是否自动切换
9. **quota exhausted 测试结果** - 是否自动切换
10. **429 cooldown 测试结果** - 是否自动切换
11. **hard limit 测试结果** - 是否自动跳过
12. **管理接口测试结果** - 所有接口是否正常
13. **普通模型兼容性测试结果** - 是否受影响
14. **发现的问题和修复记录** - 详细记录
15. **最终是否可以进入个人长期使用** - 结论

---

## 当前状态

- ✅ 第七阶段测试框架已创建
- ✅ fallback.json 已配置为 OpenRouter
- ⏳ 等待用户提供 One API 管理员 Token 和确认 OpenRouter 渠道配置
- ⏳ 开始实际测试
